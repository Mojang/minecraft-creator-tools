{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "item/floatrange",
  "title": "Item FloatRange",
  "description": "Specifies a numeric range between minimum and maximum values for randomized item properties. Used for variable durability, damage ranges, or timing intervals. The game picks a random value within the range when the property is evaluated, adding natural variation to item behavior.",
  "type": "object",
  "properties": {
    "max": {
      "title": "Max",
      "type": "number",
      "default": 0
    },
    "min": {
      "title": "Min",
      "type": "number",
      "default": 0
    }
  }
}