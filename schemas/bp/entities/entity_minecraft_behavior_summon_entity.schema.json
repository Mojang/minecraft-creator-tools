{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Entity Summon Entity Behavior",
  "description": "Allows the mob to attack the player by summoning other entities.",
  "properties": {
    "summon_choices": {
      "title": "Summon Choices",
      "description": "List of spells for the mob to use to summon entities.",
      "type": "array",
      "items": {
        "$ref": "./entity_summonactorgoaldefinition_spelldata.schema.json"
      }
    },
    "control_flags": {
      "title": "Control Flags",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "jump",
          "look",
          "move"
        ]
      }
    },
    "priority": {
      "title": "Priority",
      "description": "As priority approaches 0, the priority is increased. The higher the priority, the sooner this behavior will be executed as a goal.",
      "type": "integer",
      "default": 0
    }
  }
}