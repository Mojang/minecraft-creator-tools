{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Loot",
  "description": "Specifies the loot table that determines what items this entity drops upon death. The table path is relative to the behavior pack's root folder.",
  "properties": {
    "table": {
      "title": "Table",
      "description": "Path to the loot table JSON file, relative to the behavior pack's root (e.g., 'loot_tables/entities/zombie.json').",
      "type": "string"
    }
  }
}