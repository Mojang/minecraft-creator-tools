{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Block Item Specific Speed",
  "description": "Defines mining speed bonuses for specific tool items when breaking this block. Map item identifiers to speed multipliers so diamond pickaxes mine faster than stone, or custom tools have specialized efficiency. Enables proper tool tier progression and specialized harvesting tools.",
  "properties": {
    "destroy_speed": {
      "title": "Destroy Speed",
      "description": "Mining speed for that specific item. A negative number means the block can't be mined with that item.",
      "type": "number"
    },
    "item": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      ],
      "title": "Item",
      "description": "The item with a specific mining speed."
    }
  },
  "required": [
    "destroy_speed",
    "item"
  ]
}