{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Constraints",
  "description": "Specific constraints that must be satisfied when placing this structure.",
  "type": "object",
  "properties": {
    "grounded": {
      "title": "Grounded",
      "description": "When specified, ensures the structure's ground level touches the ground.",
      "type": "object"
    },
    "leveled": {
      "title": "Leveled",
      "description": "When specified, ensures the structure's ground level is placed on mostly flat terrain.",
      "type": "object",
      "properties": {
        "max_steepness": {
          "title": "Max Steepness",
          "description": "Maximum allowed height difference between the placement position and terrain samples taken halfway along each side of the structure. A valid terrain sample consists of a solid block with a non‑solid block above it. Defaults to 2.",
          "type": "integer"
        }
      },
      "defaultSnippets": [
        {
          "label": "Leveled",
          "description": "When specified, ensures the structure's ground level is placed on mostly flat terrain.",
          "body": {
            "max_steepness": 0
          }
        }
      ]
    },
    "unburied": {
      "title": "Unburied",
      "description": "When specified, ensures the structure has air above it.",
      "type": "object"
    },
    "block_intersection": {
      "title": "Block Intersection",
      "description": "When specified, ensures the structure only intersects with allowlisted blocks.",
      "type": "object",
      "properties": {
        "block_allowlist|block_whitelist": {
          "title": "Block Allowlist|block Whitelist",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "only_check_intersection_for_motion_blocking_blocks": {
          "title": "Only Check Intersection For Motion Blocking Blocks",
          "description": "If true, only motion-blocking blocks within the structure are checked for intersections with blocks in \"block_allowlist|block_whitelist\". If false, all blocks in the structure except empty ones are checked for intersections, including air.",
          "type": "boolean"
        }
      },
      "defaultSnippets": [
        {
          "label": "Block Intersection",
          "description": "When specified, ensures the structure only intersects with allowlisted blocks.",
          "body": {
            "block_allowlist|block_whitelist": "",
            "only_check_intersection_for_motion_blocking_blocks": true
          }
        }
      ]
    }
  }
}