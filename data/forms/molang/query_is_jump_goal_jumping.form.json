{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "checks if entity is doing a jump goal jump"
          }
        ]
      }
    ]
  },
  "id": "query.is_jump_goal_jumping",
  "fields": [],
  "description": "Returns 1.0 if the entity is doing a jump goal jump, else it returns 0.0.",
  "title": "Is Jump Goal Jumping"
}