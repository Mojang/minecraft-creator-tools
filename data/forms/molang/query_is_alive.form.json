{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "checks if entity is alive, and 0.0 if it is dead"
          }
        ]
      }
    ]
  },
  "id": "query.is_alive",
  "fields": [],
  "description": "Returns 1.0 if the entity is alive, and 0.0 if it's dead.",
  "title": "Is Alive"
}