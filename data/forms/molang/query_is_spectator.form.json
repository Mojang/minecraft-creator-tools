{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "checks if entity (player) is in spectator mode"
          }
        ]
      }
    ]
  },
  "id": "query.is_spectator",
  "fields": [],
  "description": "Returns 1.0 if the entity (player) is in spectator mode, else it returns 0.0.",
  "title": "Is Spectator"
}