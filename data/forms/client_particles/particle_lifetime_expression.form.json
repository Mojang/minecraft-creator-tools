{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "controls particle expiration and lifetime via expressions"
          }
        ]
      }
    ]
  },
  "fields": [
    {
      "id": "expiration_expression",
      "title": "Expiration Expression",
      "description": "When the expression is non-zero, the particle expires. Can be a number or Molang expression.",
      "dataType": "molang",
      "defaultValue": 0,
      "alternates": [
        {
          "id": "expiration_expression",
          "dataType": "keyedObjectCollection",
          "readOnly": true,
          "subForm": {
            "id": "expiration_expression",
            "title": "Expiration Expression",
            "fields": [
              {
                "id": "expression",
                "title": "Expression",
                "dataType": "string",
                "isRequired": true
              },
              {
                "id": "version",
                "title": "Version",
                "dataType": "int",
                "isRequired": true
              }
            ]
          }
        },
        {
          "id": "expiration_expression",
          "dataType": "float"
        }
      ]
    },
    {
      "id": "max_lifetime",
      "title": "Max Lifetime",
      "description": "The maximum lifetime of the particle in seconds. Can be a number or Molang expression.",
      "dataType": "molang",
      "alternates": [
        {
          "id": "max_lifetime",
          "dataType": "keyedObjectCollection",
          "readOnly": true,
          "subForm": {
            "id": "max_lifetime",
            "title": "Max Lifetime",
            "fields": [
              {
                "id": "expression",
                "title": "Expression",
                "dataType": "string",
                "isRequired": true
              },
              {
                "id": "version",
                "title": "Version",
                "dataType": "int",
                "isRequired": true
              }
            ]
          }
        },
        {
          "id": "max_lifetime",
          "dataType": "float"
        }
      ]
    }
  ],
  "description": "Controls individual particle lifetimes using Molang expressions. The max_lifetime sets the absolute duration limit, while expiration_expression can terminate particles early when conditions are met (like leaving a certain area). Enables dynamic particle populations that respond to game state beyond simple time-based expiration.",
  "title": "Client Particles Particle Lifetime Expression",
  "id": "particle_lifetime_expression"
}