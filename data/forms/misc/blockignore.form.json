{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "specifies block types in the template that should be ignored by other processors"
          }
        ]
      }
    ]
  },
  "id": "BlockIgnore",
  "fields": [
    {
      "id": "processor_type",
      "title": "Jigsaw Processor Type",
      "dataType": "string",
      "isRequired": true
    },
    {
      "id": "blocks",
      "title": "Blocks",
      "description": "List of blocks evaluated on all blocks of a Structure Template. If a block is matched in the provided list the block will be ignored for processing.",
      "dataType": "objectArray",
      "isRequired": true
    }
  ],
  "description": "Specifies block types in the template that should be ignored by other processors.",
  "title": "BlockIgnore Misc"
}
