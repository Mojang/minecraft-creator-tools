# Security: skip all lifecycle scripts from dependencies by default.
# After `npm ci`, run `npm run rebuild-deps` to execute lifecycle scripts
# ONLY for the explicitly allowed packages listed below.
#
# Allowed packages:
#   esbuild - downloads platform-specific native binary
ignore-scripts=true
